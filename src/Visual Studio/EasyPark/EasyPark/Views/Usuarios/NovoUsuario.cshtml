@model EasyPark.EasyPark.Domain.Requests.UsuariosRequest;
@{
    ViewData["Title"] = "Novo cadastro no sistema";
}

<div class="row">
    <div class="col-md-12">
        <h2>Crie sua conta</h2>
        <p class="text-justify">
            Olá, utilize essa tela para realizar seu cadastro. Por gentileza, se atente ao preenchimento dos dados pois você poderá escolher qual será o tipo de usuário a ser criado.
            <br />Uma vez criado, não poderá alterar o nível de acesso.
        </p>
    </div>
</div>
<br />
<br />
<form asp-action="CadastrarNovoUsuario" method="Post" onsubmit="return;true">
    <div class="row">
        <div class="col-md-6">
            <label for="nivelAcesso" id="LblNivelAcesso" class="control-label">Selecione o tipo de cadastro</label>
            <select id="CbNivelAcesso" class="dropdown" asp-for="@Model.NivelAcesso">
                <option value=1>Usuário comum</option>
                <option value=2>Prestador de serviço</option>
                <option value=3>Dono de estacionamentos</option>
            </select>
        </div>
        <div class="col-md-6">
            <label for="nome" id="LblNome" class="control-label">Nome completo</label>
            <input type="text" id="nome" class="form-group" asp-for="@Model.NomeUsuario" />
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <label for="email" id="LblEmail" class="control-label">Email</label>
            <input type="email" id="email" class="form-group" asp-for="@Model.Email" placeholder="Informe o e-mail corretamente pois ele será sua forma de autenticação." />
        </div>
        <div class="col-md-6">
            <label for="senha" id="LblSenha" class="control-label">Senha</label>
            <input type="password" id="senha" class="form-group" asp-for="@Model.Senha" />
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <input type="reset" class="btn btn-outline-danger" value="Limpar dados" />
        </div>
        <div class="col-md-6">
            <input type="submit" class="btn btn-success" value="Criar conta" />
        </div>
    </div>
</form>